# CC harness support

Shared provider-option policy and checkpointable adapters for read/write/edit/bash.
Used by the basic coding and environments harnesses. The prepared filesystem plan
must be persisted before execute; ambiguous errors preserve that same plan. The
caller owns observations, host selection, steering, retries, and loop semantics.
This package is not a registered harness or a universal worker runtime.
